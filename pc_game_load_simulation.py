fps = [30,60,120]
game = [0.35,2,3]
gpu = []
desired_game = game[int(input("Choose game load level(1-3): csgo(level 1 load), witcher 3(level 2 load), pubg(level 3 load)"))-1]
desired_fps = fps[int(input("How much fps do you need on a scale of 1 to 3(30, 60, or 120)?"))-1]
desired_gpu = gpu[int(input("Your gpu(from 1 to 3): gtx 980, gtx 1050ti, gtx 1080ti"))-1]
def work():
    temp = 
def playing(desired_fps):
    

    
while playing == True:
    